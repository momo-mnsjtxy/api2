<?php

/**
 * Optional explicit routes.
 * Default routing is PATHINFO: /{module}/{controller}/{action}
 * Kept for documentation / future custom aliases.
 */
return [
    // 'GET /' => 'index/index/index',
];
